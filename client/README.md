# Client

Browser-first UI for camera-based open-vocabulary guidance.

## Folders
- `public/`: static entry (`index.html`, icons).
- `src/`: UI logic (capture, detection pipeline, overlay, audio guidance).
- `assets/`: fonts, shapes, mock data for HUD or tutorials.
