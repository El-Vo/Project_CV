# API Endpoints

Organize functions that proxy to Hugging Face/Replicate or serve config bundles to the client.

Examples:
- `infer/`: lightweight POST to sample open-vocabulary endpoint.
- `health/`: status for readiness probes.
